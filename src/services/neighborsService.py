def count_neighbors(table_data):
	# for x in range(len(table_data)):
	# 	for y in range(len(table_data[x])):
	# 		slot = table_data[x][y]
	# 		if slot.is_bomb:
	# 			continue

	# 		slots.neighbors_count = count_neighbors(slot)

	return table_data


def get_neighbors(slot):
	# pos = { 'x': slot.x, 'y': slot.y }
	# neighbors = (
		
	# )
	return 4
